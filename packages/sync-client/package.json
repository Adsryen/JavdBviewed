{
  "name": "@javdb/sync-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloud sync client engine (no chrome / DOM APIs)",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@javdb/sync-protocol": "workspace:*"
  },
  "files": [
    "src"
  ]
}
