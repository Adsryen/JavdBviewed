# Todo 计划

> 这个文档按版本规划维护：先判断版本级别，再在版本内划分优先级。小内容进入既有版本池，只有形成明确发布主题并通过发版门槛后才发布新版本。

---

## 版本号分级规则

### Patch：`1.20.x`

适用于兼容性修复、稳定性收口、小范围体验优化和已上线功能补齐。

- 修复测试、构建、发布说明、回归问题
- 修复 WebDAV、Emby/Jellyfin、番号库、磁力等已发布能力的问题
- 小范围 UI 文案、提示、选择器校准、设置项默认值调整
- 不改变用户数据结构的轻量功能补齐

发布判断：同一主题下的 P0 阻断项清零，P1 收口项完成，验证链路通过。

### Minor：`1.21.0+`

适用于完整新能力、跨模块工作流、可感知的功能组合和中等架构调整。

- 新增一组完整用户能力，并且用户能感知到一条完整工作流
- 需要新增设置项、数据流、后台任务、页面入口和测试覆盖
- 影响多个功能域，但保持旧配置、旧数据、旧入口兼容
- 架构调整服务于一个明确功能主题
- 同一功能需要拆成多个 Patch 铺垫时，最后一个整合版本才考虑升 Minor

发布判断：连续多个 `1.20.x` 小版本已经完成铺垫，并形成一组完整新能力时，再进入 `1.21.0`。

### Major：`2.0.0`

适用于不兼容变更、存储边界重构、权限模型变化和旧入口移除。

- 移除旧兼容路径、旧配置入口或旧数据结构
- 改变用户数据导入、备份、恢复、同步的兼容规则
- 引入需要用户重新授权或重新配置的权限变化
- 重做核心导航、设置结构或跨页面工作流

发布判断：需要迁移说明、回滚策略、数据备份提醒和跨版本升级测试。

---

## 版本内优先级

- P0：发布阻断。测试失败、构建失败、数据风险、迁移风险、发布说明缺失。
- P1：版本核心。这个版本承诺交付的主线功能或主线收口。
- P2：随版增强。低风险体验优化、文案、提示、轻量 UI、局部校准。
- P3：评估池。需要调研、拆分设计或等待稳定周期的事项。

---

## 发版门槛

- [x] `node scripts/assert-release-notes.cjs <version>` 通过
- [x] `pnpm run typecheck` 通过
- [x] `pnpm test` 通过
- [x] `pnpm run test:dom` 通过
- [x] `pnpm run build` 通过并生成对应 `dist-zip` 产物
- [x] `version.json`、`package.json`、`src/manifest.json` 版本一致
- [ ] 工作区只保留本次发布需要提交的文件
- [ ] GitHub Release notes 覆盖用户可感知变化、风险提示和产物 SHA256

---

## v1.20.3：发布收口

版本级别：Patch。

发布主题：把 `v1.20.2..HEAD` 已完成的大批架构迁移、WebDAV、Emby/Jellyfin、番号库和设置增强整理成一个稳定发布。

### P0：发布阻断

- [x] 更新发布公告测试
  - [x] `tests/extension/releaseAnnouncementState.test.ts` 最新版本断言包含 `1.20.3`
  - [x] 确认 `src/features/releaseAnnouncement/domain/releaseNotes.ts` 的 `1.20.3` 文案至少 3 条用户可读亮点
- [x] 修复或调稳 regression 超时
  - [x] `tests/regression/orchestratorDoubleManaged.test.ts`
  - [x] `tests/regression/getSettingsListEnhancement.test.ts`
  - [x] `tests/regression/dataAggregatorDefaults.test.ts`
- [x] 修复或调稳 extension 测试失败
  - [x] `tests/extension/webdavBaseline.test.ts`
  - [x] `tests/extension/webdavCleanupService.test.ts`
  - [x] `tests/extension/telemetryErrorPayload.test.ts`
- [x] 确认 `pnpm run test:dom` 在 WSL/OneDrive 沙箱路径下需沙箱外执行，完整 DOM 套件通过
- [x] 收口构建副作用
  - [x] 确认 `version.json` build `105` 作为当前发布候选起点
  - [x] 确认行尾变化导致工作区存在大量改动，发布前以本次版本文件为提交边界
  - [x] 在真实 Git 工作区重新生成 `dist-zip/javdb-extension-v1.20.3-build-106.zip`

### P1：本版发布内容

- [x] WebDAV 备份文件支持按设备筛选和清理
- [x] WebDAV 新增清单、系列、番号备份和恢复能力
- [x] WebDAV 恢复支持按类别选择合并或覆盖策略
- [x] WebDAV 合并恢复按记录标识去重，覆盖恢复先清空同类数据
- [x] WebDAV 恢复页面补齐清单 / 系列 / 标签类别显示
- [x] WebDAV 恢复页面完成状态模型拆分重构
- [x] Emby/Jellyfin 新增媒体库入库状态显示
- [x] Emby/Jellyfin 自动识别已配置服务器地址
- [x] 设置页按“页面增强”和“媒体库入库状态”分组
- [x] 设置搜索增强，支持分类管理和高亮显示
- [x] 列表页状态快捷标识支持开关，默认关闭
- [x] 列表页快捷标识支持已浏览、想看、已观看写入本地番号库
- [x] 番号库使用 IndexedDB 持久化
- [x] 多源磁力聚合、失败退避和状态标签优化
- [x] 详情页新增外部搜索和字幕搜索入口
- [x] 演员元数据刷新增加维基数据获取失败诊断
- [x] Dashboard 恢复页、报告、记录、演员、新作品标签页完成组件化拆分
- [x] 扩展错误事件上报和功能指标统计
- [x] 修复请求调度器 `fetch` 上下文问题

### P2：发布说明与风险提示

- [x] Release notes 标明这是 `1.20.x` 内的大型稳定收口版本
- [x] 提醒 WebDAV、IndexedDB、Emby/Jellyfin 用户更新前完成一次手动备份
- [x] 记录动态 import chunk 警告为后续性能优化项

### 发布信息草稿

版本定位：`1.20.x` 大型稳定收口 Patch。

产物：`dist-zip/javdb-extension-v1.20.3-build-106.zip`

SHA256：`6551a6ec9044ac4985874156d175c19ff5147ffc8c19624361dcbc6f6ee18ed3`

Release notes：

- WebDAV 支持按设备筛选、清理和恢复备份，并补齐清单、系列、番号数据恢复链路。
- WebDAV 恢复支持按类别选择合并或覆盖，合并恢复会按记录标识去重并保留本地独有数据。
- Emby/Jellyfin 新增媒体库入库状态、服务器识别和设置分组。
- 设置搜索增强，列表页新增状态快捷标识，番号库改用 IndexedDB 持久化。
- 磁力搜索支持多源聚合、失败退避和状态标识优化。
- 详情页新增外部搜索和字幕搜索入口。
- 演员元数据刷新增加维基失败诊断。
- Dashboard 恢复页、报告、记录、演员、新作品标签页完成组件化拆分。

风险提示：

- WebDAV、IndexedDB、Emby/Jellyfin 用户更新前先完成一次手动备份。
- 构建仍有动态 import chunk 警告和大 chunk 提示，作为后续性能优化项跟踪。
- 当前构建产物带有 `dirty` 版本标记，正式发布前提交本版改动后再执行一次 clean build。

当前验证：

- [x] `/tmp/javdbviewed-test`：`pnpm run typecheck`
- [x] `/tmp/javdbviewed-test`：`pnpm test`
- [x] `/tmp/javdbviewed-test`：`pnpm run test:dom`
- [x] `/tmp/javdbviewed-test`：`pnpm run build`
- [x] 真实 Git 工作区：`node scripts/assert-release-notes.cjs 1.20.3`
- [x] 真实 Git 工作区：`pnpm run typecheck`
- [x] 真实 Git 工作区：`pnpm test`
- [x] 真实 Git 工作区：`pnpm run test:dom`
- [x] 真实 Git 工作区：`pnpm run build`
- [x] 真实 Git 工作区：计算最终 zip SHA256

---

## v1.20.4：稳定补丁

版本级别：Patch。

发布主题：承接 `1.20.3` 发布后的问题修复和低风险体验补齐；不引入新的数据结构。

### P0：线上问题修复

- [ ] 处理 `1.20.3` 发布后反馈的安装、升级、构建、发布公告问题
- [ ] 处理 WebDAV 备份/恢复/清理相关数据问题
- [ ] 处理 Emby/Jellyfin 设置兼容和媒体库索引问题
- [ ] 处理 IndexedDB 番号库迁移、查询、标签统计回归问题

### P1：低风险功能补齐

- [ ] WebDAV 设置：Alist URL 规范化提示
  - [ ] 检测常见 Alist `/dav/` 路径问题
  - [ ] 给出可操作的修正提示
- [ ] 在线可看站点校准
  - [ ] 持续按误报样本修正 FANZA、Jable 等站点 selector
  - [ ] 失败站点显示逻辑继续受设置项控制
- [ ] 日志设置：检查所有功能增强是否具备独立标识

### P2：体验优化

- [ ] 新作品：添加演员弹窗优化
  - [ ] 弹窗列出的列表添加标识排除拉黑演员

---

## v1.20.5：页面识别基础

版本级别：Patch。

发布主题：先把番号识别能力抽成可复用基础能力，只接入低风险入口。

### P0：规格与边界

- [ ] 页面番号识别规格
  - [ ] 定义可识别区域：影片页评论、媒体服务器页面、JavDB 列表/详情页
  - [ ] 定义经典番号、FC2、无码、含前后缀噪声文本的解析规则
  - [ ] 定义与 Emby/Jellyfin 入库状态、外部搜索、字幕搜索的复用接口

### P1：主线功能

- [ ] 页面番号识别
  - [ ] 在影片页识别网友评论中的经典番号
  - [ ] 抽出可复用番号提取逻辑，供媒体库索引、媒体服务器页面和 JavDB 页面标记共用
  - [ ] 与 Emby/Jellyfin 相关入口形成可复用能力

### P2：同版增强

- [ ] 字幕预览入口评估
  - [ ] 评估迅雷字幕 `.srt` 预览收益
  - [ ] 控制预览加载范围，避免请求和 UI 复杂度过高
- [ ] 123AV FC2 专用入口
  - [ ] 评估是否在 FC2 场景补 123AV 入口
  - [ ] 与当前 FC2 弹窗入口保持一致体验

---

## v1.20.6：列表与状态体验优化

版本级别：Patch。

发布主题：优化列表页、详情页和番号库的状态管理体验；保持现有数据结构兼容。

### P0：规格与边界

- [ ] 标签高亮/折叠规格
  - [ ] 复用现有内容过滤规则
  - [ ] 定义高亮、折叠、隐藏三类行为边界
  - [ ] 定义默认关闭、用户主动开启的设置策略

### P1：主线功能

- [ ] 标签高亮/折叠
  - [ ] 支持列表页按标签高亮命中项
  - [ ] 支持折叠低优先级标签区域
  - [ ] 与现有内容过滤规则联动
- [ ] 状态按钮视觉优化
  - [ ] 详情页当前状态高亮，其它状态弱化
  - [ ] 评估 JAV-JHS 的屏蔽、收藏、已下载、已观看四状态交互

### P2：同版增强

- [ ] 高质量磁力过滤（来源：xie1995 fork 评估 #2，2026-07-03）
  - 版本级别：Patch
  - 优先级：P2
  - 发布价值：给磁力搜索结果自动打分（0-100）并排序，帮助用户快速识别高质量资源。评分维度：做种比 30%、文件大小合理性 25%、画质/字幕关键词 25%、上传者信誉 10%、文件名完整性 10%
  - 风险面：低，纯增量功能，~265 行纯函数无侵入性，与现有 `MagnetResult` 类型完全兼容
  - 实现方式：直接取用 `qualityScore.ts` + `domain/config.ts`（含并发控制设置），接入点在 `magnetSearchManager.ts` 结果展示环节
  - 验证方式：单元测试覆盖评分逻辑，手动验证磁力搜索结果排序
- [ ] 列表页收藏按钮（来源：xie1995 fork 评估 #4，2026-07-03）
  - 版本级别：Patch
  - 优先级：P2
  - 发布价值：在番号列表封面图上加 ❤️ 按钮，支持一键收藏/取消收藏，不用逐个进详情页
  - 风险面：UI，需适配现有列表渲染逻辑
  - 实现方式：按现有 `listEnhancement` 模块风格重写，利用已有的 `VideoRecord.isFavorite` / `favoritedAt` 字段
  - 验证方式：列表页点击收藏按钮验证状态切换和持久化
- [ ] 番号库状态筛选 UX 优化

### P3：本版评估项

- [ ] 聚合能力定义
  - [ ] 明确”聚合”指数据源聚合、搜索结果聚合、报告聚合或页面信息聚合
  - [ ] 拆成独立版本主题后再进入 Minor 版本
- [ ] 轻量图片画廊（已被 v1.21.0+ “外部预览图功能” P1 覆盖，此条归档）
  - [ ] 评估 ViewerJS 或现有预览能力复用

---

## v1.20.7：媒体服务器体验收口

版本级别：Patch。

发布主题：继续完善 Emby/Jellyfin 使用体验，避免做大规模命名迁移。

### P0：兼容边界

- [ ] 保留 `settings.emby`、`emby_library_state` 等存储兼容入口
- [ ] 保持现有 Emby/Jellyfin 页面番号链接化和快捷搜索能力稳定
- [ ] 确认媒体库索引、实时校验、页面标记升级路径兼容旧配置

### P1：主线功能

- [ ] Emby/Jellyfin 实时媒体库校验队列收口
  - [x] 内容页收集当前可见番号后发送 `EMBY_LIBRARY_CHECK_CODES`
  - [x] 后台按并发 1、批量上限 20、短期去重缓存执行查询
  - [x] 校验结果修正本地索引：查到则 upsert，查无则只删除该服务器该番号的旧条目
  - [ ] 列表页收到结果后增量更新对应卡片，避免整页重复处理

### P2：媒体服务器体验优化

- [ ] Emby 番号变体匹配（来源：xie1995 fork 评估 #5a，2026-07-03）
  - 版本级别：Patch
  - 优先级：P2
  - 发布价值：同一番号在 Emby 中可能以 `ABC-123`、`ABC123`、`abc_123` 等形式存在，变体匹配提高入库命中率
  - 风险面：低，~20 行增量改动，不动现有类型和存储结构
  - 实现方式：将 `matcher.ts` 的 `normalizeCode` + `generateCodeVariants` 逻辑合入现有 `libraryIndex.ts` 查询函数，在查询时做变体匹配
  - 验证方式：测试不同格式番号的匹配命中率
- [ ] Emby 封面图直读（来源：xie1995 fork 评估 #5b，2026-07-03）
  - 版本级别：Patch
  - 优先级：P2
  - 发布价值：直接使用 Emby 原始封面图，省一次 JavDB 网络请求，JavDB 不可用时仍能显示封面
  - 风险面：低，~3 行改动，在现有 `EmbyLibraryIndexEntry` 上加可选字段 `coverImageUrl?: string`
  - 实现方式：构建索引时从 Emby API 顺便存封面 URL
  - 验证方式：确认 Emby 入库的番号优先使用 Emby 封面
- [ ] Emby storage.onChanged 自动刷新（来源：xie1995 fork 评估 #5c，2026-07-03）
  - 版本级别：Patch
  - 优先级：P2
  - 发布价值：Emby 同步完数据后 dashboard 自动更新状态，不用手动刷新页面
  - 风险面：低，~15 行改动，在 bootstrap 加监听器
  - 实现方式：在 `src/apps/background/bootstrap.ts` 中注册 `chrome.storage.onChanged` 监听 Emby 相关 key 变化
  - 验证方式：手动触发 Emby 后台同步，确认 dashboard 自动刷新
- [ ] 增强媒体服务器页面对页面番号识别结果的入口展示
- [ ] 对媒体库同步错误提供更清晰的设置页诊断信息

---

## v1.20.8：报告、同步与资源优化

版本级别：Patch。

发布主题：补齐报告参数、后台同步请求瘦身和资源入口小功能。

### P1：主线功能

- [ ] 报告（Insights）设置
  - [ ] 配置报告生成所用的聚合参数
  - [ ] 仅影响本地统计和 AI 提示词输入
- [ ] 数据同步请求瘦身
  - [ ] 评估后台同步、批量刷新、WebDAV 恢复等流程是否可以拦截图片与视频资源请求
  - [ ] 降低原站压力，提高后台请求速度
  - [ ] 覆盖正常页面加载和后台数据请求的差异测试

### P2：资源入口优化

- [ ] DMM/FANZA 预告片多画质
  - [ ] 评估 DMM API 稳定性
  - [ ] 决定是否替换 JavDB 原生预告片源

---

## v1.21.0+：中版本候选池

版本级别：Minor 候选。

这些项需要先通过 `1.20.x` 小版本完成铺垫。只有形成完整新能力、跨模块工作流或明确架构治理主题时，才升到 `1.21.0+`。

### P0 候选

- [ ] Emby/Jellyfin 命名迁移设计
  - [ ] 规划内部文件及模块命名从 `embyEnhancement`、`EmbySettings`、`emby-settings` 迁移到媒体服务器语义
  - [ ] 梳理动态导入、设置路由、内容脚本兼容导出和回归测试覆盖
- [ ] 旧兼容出口清理计划（来源：xie1995 fork 评估 #8，2026-07-03）
  - 版本级别：Minor
  - 优先级：P0
  - 发布价值：删除 72 个冗余文件（`src/background/` 19 个空壳 + `src/services/` 53 个已迁移文件），消除旧代码干扰
  - 风险面：架构，需迁移 17+ 个 dashboard 文件的 import 路径（`services/` → `features/`）
  - 实现方式：1) 先迁移引用再删文件；2) `background.ts` 仅一行重定向可直接删；3) 配合配置拆分一起做
  - 验证方式：全量 `pnpm run typecheck` + `pnpm test` + `pnpm run build` 通过
- [ ] 配置拆分重构（来源：xie1995 fork 评估 #7，2026-07-03）
  - 版本级别：Minor
  - 优先级：P0
  - 发布价值：将 `config.ts`（712 行）中各模块默认值拆到对应 `features/*/domain/config.ts`，提升模块自包含性
  - 风险面：架构，涉及 ~20 个文件 import 路径改动
  - 实现方式：逐模块迁移，先拆改动最频繁的模块，`config.ts` 变为纯组装层。与旧兼容出口清理同步进行
  - 验证方式：每拆一个模块跑一次 `pnpm run typecheck` + `pnpm test`

### P1 候选

- [ ] 外部预览图功能（来源：xie1995 fork 评估 #1，2026-07-03）
  - 版本级别：Minor
  - 优先级：P1
  - 发布价值：从 javfree.me / javstore.net 抓取预览截图，在列表页和详情页展示，扩展预览图来源
  - 风险面：网络，外部站点可能改版或下线需维护选择器；~2200 行新代码（清理后约 1500 行）
  - 实现方式：1) 只取 `javFreeSource.ts` 和 `javStoreSource.ts` 两个数据源；2) 复用已有 `blogJav.ts`；3) 选 `externalPreviewService.ts` 而非 `fetcher.ts`；4) UI 按现有 `previews/` 模块风格重写集成；5) 需新增后台消息代理绕防盗链
  - 验证方式：手动测试三个数据源的图片抓取，确认列表页和详情页展示正常
- [ ] 收藏自动推 115（来源：xie1995 fork 评估 #4b，2026-07-03）
  - 版本级别：Minor
  - 优先级：P1
  - 发布价值：收藏番号时自动抓取磁力链接，选最优推送到 115 网盘，与磁力质量评分联动
  - 风险面：中，跨模块耦合（收藏 → 磁力搜索 → 质量评分 → 115 推送），链条长
  - 实现方式：作为独立可选功能，通过设置项控制，不嵌在收藏按钮里。利用 `selectOptimalMagnet` + `qualityScore` 选择最优磁力
  - 验证方式：开启设置后收藏一个番号，确认自动推送流程完整执行
- [ ] 演员库新增演员更多信息
  - [ ] 可通过增强功能获取 Wiki 信息并保存
- [ ] 从 JavDB 收藏演员导入
  - [ ] 评估 JavDB 收藏演员数据来源
  - [ ] 导入时去重并保留现有演员备注
- [ ] 演员库列表视图和卡片视图切换优化

### P2 候选

- [ ] 聚合能力
  - [ ] 当数据源聚合、搜索结果聚合、报告聚合能组成完整工作流时，规划为 Minor

### P3 暂缓

- [ ] 列表页短评弹窗
  - [ ] 评估接口压力和缓存策略
  - [ ] 明确缓存、频率限制和失败降级后再排版本

---

## v2.0.0：固定扩展 ID 迁移

版本级别：Major。

发布主题：把当前依赖“同一个未打包加载目录”的 1.x 扩展身份，迁移到带固定 `manifest.key` 的新扩展 ID。该版本是显式数据迁移版本，不作为普通补丁或中版本发布。

### P0：迁移前置与数据安全

- [ ] 保持 1.x 发布链路无 `manifest.key`
  - [ ] `src/manifest.json` 不含 `key`
  - [ ] `dist/manifest.json` 不含 `key`
  - [ ] ZIP 内 `manifest.json` 不含 `key`
  - [ ] 构建脚本不注入 `manifest.key`
- [ ] 发布 1.x 过渡版本
  - [ ] 提供明确的迁移前备份提示
  - [ ] 显示最近备份成功时间
  - [ ] 标明备份包含的数据类别
  - [ ] 提醒用户不要在确认备份可导入前卸载旧扩展
- [ ] 定义 2.0.0 固定 ID
  - [ ] 选择并锁定 2.0.0 key
  - [ ] 用脚本计算实际扩展 ID
  - [ ] 把固定 ID 写入任务文档和 release notes
  - [ ] 私钥离线备份，不提交仓库

### P1：2.0.0 迁移主线

- [ ] 2.0.0 引入固定 `manifest.key`
  - [ ] 仅允许在 2.0.0 分支或明确开关中启用
  - [ ] 构建后验证 manifest key 对应 ID
  - [ ] 防止 key 回流到 1.x 发布链路
- [ ] 2.0.0 首次启动迁移向导
  - [ ] 检测新 ID 下无数据时提示导入 1.x 备份
  - [ ] 支持从 1.x WebDAV 备份或本地导出文件恢复
  - [ ] 恢复设置、番号记录、演员库、列表/标签、WebDAV 配置等核心数据
  - [ ] 重复导入幂等，不产生重复记录
- [ ] 回滚路径
  - [ ] 用户可回到旧 1.x 加载目录刷新旧 ID
  - [ ] 旧 ID 数据仍在时可重新导出
  - [ ] 文档说明卸载旧扩展可能导致旧数据被 Chrome 清理

### P2：文档与发布体验

- [ ] 写完整迁移文档
  - [ ] 为什么 2.0.0 会出现新 ID
  - [ ] 固定 ID 的好处：跨目录加载仍访问同一份数据
  - [ ] 迁移前备份步骤
  - [ ] 迁移后导入步骤
  - [ ] 常见错误：加载错目录、未备份、卸载旧扩展、导入失败
- [ ] Release notes 强提示
  - [ ] 这是不兼容 Major 升级
  - [ ] 旧 ID 数据不会被 Chrome 自动迁移
  - [ ] 必须先备份再安装固定 ID 版本

### P3：暂缓/不做

- [ ] 不尝试生成匹配旧 ID `niagboijeoecgdflgjgdajedfohlodaa` 的 key
  - [ ] 完整 32 字符扩展 ID 等价约 128-bit 预映射
  - [ ] 暴力搜索实际不可行

Trellis 任务：`.trellis/tasks/07-03-v200-fixed-extension-id-migration`

---

## 新待办入池模板

新增待办时按这个模板补齐，方便判断版本号级别。

```md
- [ ] 功能/修复名称
  - 版本级别：Patch / Minor / Major
  - 优先级：P0 / P1 / P2 / P3
  - 发布价值：
  - 风险面：数据 / 设置 / UI / 网络 / 架构 / 测试
  - 验证方式：
```

---

## 当前发版建议

- `1.20.3`：验证通过，可进入发布；正式产物建议在提交本版改动后 clean build 一次。
- `1.20.4`：稳定补丁，只接修复和低风险补齐。
- `1.20.5`：页面番号识别基础。
- `1.20.6`：列表与状态体验优化。新增：磁力质量评分（P2）、列表页收藏按钮（P2），均来自 xie1995 fork 评估。
- `1.20.7`：媒体服务器体验收口。新增：Emby 变体匹配 / 封面直读 / 自动刷新（P2），增量改动共约 50 行。
- `1.20.8`：报告、同步与资源优化。
- `1.21.0+`：中版本候选池。新增：外部预览图（P1）、收藏自动推 115（P1）、配置拆分 + 遗留清理（P0），均来自 xie1995 fork 评估。
- `2.0.0`：固定扩展 ID 迁移；只有备份、导入、回滚链路都完成后才发布。

### xie1995 fork 对比评估记录（2026-07-03）

对比仓库：`https://github.com/xie1995/javdb`（fork 自本仓库）

已评估 9 项改动，采纳 6 项（#1 #2 #4 #5 #7 #8），不采纳 3 项（#3 前置依赖未满足、#6 已规划 2.0.0、#9 无实质改进）。

核心原则：我们是主仓库，只采纳有价值的思想和实现，不做类型重构或大规模结构调整。所有改动以增量方式融入现有架构。
