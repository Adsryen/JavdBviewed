{
  "name": "@javdb/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Desktop client skeleton (no product UI yet)",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@javdb/sync-client": "workspace:*",
    "@javdb/sync-protocol": "workspace:*"
  }
}
