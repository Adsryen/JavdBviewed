{
  "name": "@javdb/tv",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TV client skeleton (no product UI yet)",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@javdb/sync-client": "workspace:*",
    "@javdb/sync-protocol": "workspace:*"
  }
}
